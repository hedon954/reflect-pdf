---
description: Swift/SwiftUI 编码规范：MainActor 使用、PDFKit 交互、Rust 调用方式
globs: **/*.swift
alwaysApply: false
---

# Swift 编码规范

## 层间约束

- **Views** 只持有 `@StateObject` / `@ObservedObject`，不直接调用 `BridgeService`
- **Coordinator / Service** 负责调用 `BridgeService`，Views 通过 ViewModel 观察状态
- **禁止**在 View 或 Coordinator 中直接使用 `URLSession`、`sqlite3` 等

## 并发

- 所有 UI 更新必须在 `@MainActor` 上执行
- Rust async 函数通过 `await` 调用（UniFFI 自动处理线程切换）
- 长时间操作用 `Task { ... }`，避免阻塞主线程

```swift
// ✅ 正确
@MainActor
class PDFInteractionCoordinator: ObservableObject {
    func processSelection() async {
        let result = try await BridgeService.shared.translate(request)
        translationState = .loaded(result)  // 已在 MainActor
    }
}

// ❌ 错误：在非 MainActor 上更新 @Published
DispatchQueue.global().async {
    self.translationState = .loaded(result)  // 会崩溃
}
```

## PDFKit 规范

- 选词防抖：`Task.sleep(nanoseconds: 300_000_000)` 后再处理
- 句子提取在 Swift 侧完成（纯字符串操作，< 10ms），不传给 Rust
- Annotation 的 `entry_id` 存入自定义属性 key `"reflect_entry_id"`

## BridgeService

- 所有跨语言调用封装在 `BridgeService.shared` 中
- 调用方只处理 Swift 原生错误类型（UniFFI 自动转换）

## 阅读位置

- 保存时机由 `ReadingSessionService` 统一管理，监听以下通知：
  - `PDFViewDocumentChanged`：触发恢复
  - `NSApplicationWillResignActiveNotification`：触发保存
  - 文档切换前：先保存旧文档位置

## 命名约定

| 类型 | 命名 |
|------|------|
| View | `PDFReaderView`、`TranslationBubble` |
| ViewModel | `LibraryViewModel`、`VocabularyViewModel` |
| Coordinator | `PDFInteractionCoordinator` |
| Service | `BridgeService`、`AudioService`、`ReadingSessionService` |
| 状态枚举 | `TranslationBubbleState`（`.hidden` / `.loading` / `.loaded` / `.error`） |
