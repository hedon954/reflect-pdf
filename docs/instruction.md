我要做一个用于阅读 PDF 的小工具，它的核心是参考 Mac 的预览工具（界面简洁，性能好，流畅），但是不同的是（也是我要解决的核心痛点），我希望它可以划词翻译，不一样的是什么呢？第一，它这个翻译，跟普通的翻译不一样，是可以基于上下文的翻译（比如自动识别到这个词所在的句子，将整个句子一起发给 LLM 进行翻译，不止是告诉我这个词是什么意思，而且是告诉我为什么是这个意思。第二，翻译后，后一个“保存”按钮，这样我可以快速将这个单词、所在的句子、和 LLM 的解释，保存到单词本中，同时，在 PDF 中，对这个词进行高亮。这样我就可以很方便进行复习了，而且，后续重复选择这个位置的这个词的话，就不需要重复调用 LLM，可以省钱，也能减少延迟。第三，同一个词在不同的地方，意思可能相同，也可能不同，那应该进行关联，比如我第二次选择翻译这个词，那应该要能基于当前的场景给予我合适的解释，也要能召回我之前的记忆，这样，符合人脑的网状记忆，学习效率更高。经过调研后，我发现 pdf.js 选词太不准确了，很影响阅读体验和学习效率。所以我决定采用以下架构：采用 SwiftUI (前端) + Rust (后端) + UniFFI (桥接) 的强类型单体架构。

---

需要调整的是：1. 我希望 rust 后端核心逻辑这块，采用 DDD 架构；2. 当 LLM 调用失败时，可以调用成熟的翻译 API（免费的最好）来进行兜底；3. 单词卡片应该有音标和音频，可以听声音。4. 只需要保留 MVP 版本的相关设计，其他的内容，统统删除，保持 PRD 和 TDD 的简洁。5. 绘图请使用 plantuml 或者 mermaid。

---

还有一些需求：1. 要能缓存之前打开的PDF文件（支持删除）；2. 重启 APP 的时候，要自动定位到之前阅读的位置，提高阅读的延续性。

---

文档不要写得那么复杂和那么长，保留核心观点就行了，具体的实现细节，到时候直接看代码就行了。不然文档太长了。

---

在正式开始开发代码之前，请你构建相关的 github cicd pipeline 和 cursor commands, skills

---

开始开发代码吧！

---

非常好，现在有另外一个问题：我重启 APP 后，虽然有缓存之前打开的文件列表，但是打开了没有展示 PDF 的内容。
我需要明确几个需求：

1. 左侧不要放什么【文库/单词本】，这个放在上面。左侧放导航栏（展示 PDF 内容的时候）。
2. 单词本需要可以展示查询次数、出现位置数、词义、音标、句子、来源 PDF 及页码；点击可跳转定位。
3. 单词本需要可以编辑、删除。
4. 单词保存后，对应的位置需要高亮。
5. 需要支持 Mac 原生预览工具的各种高亮、划线能力。

---

几个问题：

1. 单词本的样式有点丑，我希望是 mac 风格的卡片。
2. 从单词本切换回 PDF 页面的时候，不应该从第一页开始，应该恢复之前的位置。
3. 从单词本点击单词跳转到文稿为止的时候，不应该再触发翻译逻辑。
4. 现在PDF目录是有了，但是无法点击跳转，这个功能应该跟 mac 的预览工具对齐。

---

几个问题：

1. 这个上下文的文本不要乱换行，文字样式也不对，应该是正常黑色。

![](https://hedonspace.oss-cn-beijing.aliyuncs.com/img/image-20260322230325691.png)

2. 导航栏应该随着阅读的进度而高亮对应的章节。
3. 在保存到单词本的时候，对应的单词没有高亮。
4. 单词的编辑功能不是为了添加备注，而且要修改相关的解释信息。
5. 单词卡片应该是可以点击拖拽移动的。
6. 点击单词卡片的保存到单词本后，卡片不应该消失，同时，卡片上应该直接提供删除功能。



---

几个问题：

1. 将单词从单词本删除后，应该移除对应的高亮。
2. 重启 APP 后，之前的 PDF 有记录，但是无法正常展示。
3. 好像还没加入 Mac 预览工具有的那种：高亮、划线的工具。在加入之后，当选择一个单词或者句子的时候，应该出现下拉选择列表，要么是翻译，要么是高亮，要么是划线。
4. 滑动 PDF 时，目录虽然高亮有随着位置变化，但是目录栏没有移动。

---

非常好，现在又有几个问题：

1. 下拉选择列表应该就出现在选择的单词或句子的方便，这样更显眼一点。
2. 单词弹窗长摁移动的时候不丝滑。
3. 如果已经高亮了，再点击高亮，应该是移除高亮。如果已经划线了，再点击划线，应该是移除划线。如果选择的地方，一般高亮/划线了，再点击高亮/划线，则应该将没有高亮/划线的部分合并到前面，即整体做一个高亮/划线。
4. 对于处于不同位置的同一单词，不应该读取相同的缓存，因为它们所处的语境可能不同，所以需要分别进行翻译。但是，翻译后，需要把相同单词的聚合在一起，即在单词本中展示的是一个单词，但是对应多个地方的解释。

---

好像没有实现我要的能力。当我往已经高亮的地方再次高亮的时候，是加深了，而没有移除高亮。当我往已经划线的地方再次划线的时候，是重复划线，而没有移除划线，你这个效果，是不对的。

另外，在不同地方的两个一样的单词，还是读取了一样的缓存，这不是我要的效果。对于处于不同位置的同一单词，不应该读取相同的缓存，因为它们所处的语境可能不同，所以需要分别进行翻译。但是，翻译后，需要把相同单词的聚合在一起，即在单词本中展示的是一个单词，但是对应多个地方的解释。

此外，单词弹窗长摁移动的时候依旧不丝滑。

---

高亮和划线的问题已经修复了，缓存也修复了。但是现在还是一些问题：

![](https://hedonspace.oss-cn-beijing.aliyuncs.com/img/image-20260322235943121.png)

比如这个图，原文语境选择的不对。应该是下面这一句，但是选到上面那一句了。

---

非常好，但是我觉得还是可以进一步完善一点。比如说，下面这个，虽然选择是对的，但是句子是不完整的。最好是选择一整个完整的句子。比如从上一个 `.` 到一下 `.` 或者 `?`、`!` 这种表示句子结束的标识，这样会更有助于 LLM 理解。

![](https://hedonspace.oss-cn-beijing.aliyuncs.com/img/image-20260323000432311.png)

---

![image-20260323000746767](https://hedonspace.oss-cn-beijing.aliyuncs.com/img/image-20260323000746767.png)

---

很好，但是样式需要优化一下：

![](https://hedonspace.oss-cn-beijing.aliyuncs.com/img/image-20260323001522164.png)

不需要单独的【整句译文】，就放在【原文语境】里面英文换行下面就行了。另外，这里为什么原文语境的英文会这么奇奇怪怪换行呢？是不是使其是一个连贯的句子呢？这样看着很怪。

---

非常好，但这一块的样式有点丑，这里这个信息，不需要吧。

另外，单词本中，应该也包含语境解释这部分的内容。

![](https://hedonspace.oss-cn-beijing.aliyuncs.com/img/image-20260323001956676.png)

---

现在，请你根据我的 git commit logs 以及当前代码的最新状态，创建新的 prd 和 tdd，并更新 REAMDE，同时提供安装方式。 

---

现在这个项目有以下几个 BUG，我需要你进行修复，最后修复完后，将相关的技术实现整理后放在 docs/tdd 下面

1. 跨越多行的单词翻译，保存到单词本的时候，高亮有问题，同时，模型应该对单词的拼写进行纠正。
2. 跨越多行的高亮和划线，不精确，有 BUG。

3. 进入APP的时候，目录没有定位并滑动到当前所在的章节，阅读移动的时候，目录也没有实时滑动。
4. 顶部显示当前阅读页面和总页数。
5. 划线应该是跟 mac pdf 预览工具一样的红色。
6. 上次阅读位置的记忆不及时（可以通过 command+s 来立刻触发）
7. LLM 配置的更新没有实时生效，需要重启 APP 才生效。

---

1. 显示完整的 PDF 名字；
2. 我发现重启 APP 可以恢复到之前阅读的位置，但是最小化窗口后，重新打开，就不能了，只能恢复到上次刚打开 APP 时的位置。
3. 重新打开 APP 或者最小化后重新打开窗户的时候，当前页面章节是高亮了，但是目录栏没有移动到能展示高亮的范围，还是从头开始，导致看不到高亮。

---

前 2 个问题已经修复了，但是第 3 个问题还是有问题。在我关闭整个 APP 重启的时候，是可以回到我之前阅读的位置的，但是如果我仅仅只是最小化窗口，当我重新打开 APP 的时候，还是无法回到我之前阅读的位置，相反，它回到了我上次第一次打开 APP 的位置。

---

再做 2 个小优化：

1. 在翻译的时候，如果调用 LLM 失败，我希望在单词卡片的最下方显示失败的原因，是缺乏什么配置，还是调用接口报错，还是其他原因报错，可以清晰展示出来。
2. 在划线、高亮之后，可以按 cmd+z 进行撤销。

---

再做 2 个小优化：

1. 单词卡片弹窗有点太小了，我希望它的高度自适应内容的长度，并给出一个较大的上限。
2. 在打开 APP 的时候，如果用户没有配置 LLM，则弹出配置弹窗让用户配置，所以这里默认的 url, key, model 都应该是空的，但是 url 和 model 可以用 placeholder 提示。

在你验证完成之后，请在 docs/tdd 下面新建一个技术文档进行总结，并执行 git commit。

3 个问题：

1. LLM 配置弹窗点击「保存设置」后弹窗没有消失。
2. 除了保存设置，应该还有永不提醒、稍后设置这2个选项。
3. 如果没有设置 LLM 就进行翻译，则不请求 LLM，直接调用兜底翻译。
